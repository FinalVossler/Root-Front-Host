export type Theme = {
  primary: string;
  textColor: string;
  subtleColor: string;
  errorColor: string;
  linkColor: string;
};

const theme: Theme = {
  primary: "#e68a91",
  textColor: "#4c4c4d",
  subtleColor: "#b8b9b8",
  errorColor: "red",
  linkColor: "blue",
};

export default theme;
